<%= simple_form_for [ recipe, dose ], html: {class: 'form-inline'} do |f| %>

  <%= f.input :description, label: "Quantity" %>
  <%= f.association :ingredient, include_hidden: false %>
  <%= f.button :submit, class: "button button-secondary" %>
<% end %>
